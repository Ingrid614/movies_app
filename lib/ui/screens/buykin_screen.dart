import 'package:flutter/widgets.dart';

class BuyKinScreen extends StatelessWidget{
  const BuyKinScreen({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    // TODO: implement build
    throw UnimplementedError();
  }

}